# Defendency Injection

### 의존성 주입
하나의 객체가 다른 객체의 의존성을 제공하는 방법.<br>

|용어|뜻|
|-|------|
|의존성|서비스로 **사용할 수 있는** 객체|
|주입|의존성을 사용하려는 **객체로** 전달하는 것|
사용자(객체)가 어떤 서비스(함수 혹은 변수)를 사용할 것인지 지정하는 대신, 사용자가 무슨 서비스를 이용할 것인지를 말하는 것.<br>
